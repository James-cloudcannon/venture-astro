---
import CustomSection from "@builders/custom-section/CustomSection.astro";
import Heading from "@core-elements/heading/Heading.astro";
import SimpleText from "@core-elements/simple-text/SimpleText.astro";
import Image from "@core-elements/image/Image.astro";
import Card from "@wrappers/card/Card.astro";
import Text from "@core-elements/text/Text.astro";

const {
  eyebrow = "",
  heading = "",
  description = "",
  cards = [],

  colorScheme = "inherit",
  backgroundColor,
  class: className,
  useDefaultEditableBinding = false,
  _component,
  ...htmlAttributes
} = Astro.props;
---

<CustomSection
  class:list={["feature-image-card-grid", className]}
  maxContentWidth="2xl"
  paddingHorizontal="xl"
  paddingVertical="4xl"
  colorScheme={colorScheme}
  backgroundColor={backgroundColor}
  useDefaultEditableBinding={useDefaultEditableBinding}
  {...htmlAttributes}
>
  <Heading level="h3" size="xs" alignX="center" data-prop="eyebrow" text={eyebrow} />
  <Heading level="h2" alignX="center" data-prop="heading" text={heading} />
  <SimpleText text={description} alignX="center" size="md" data-prop="description" />

  <div class="feature-image-card-grid__grid" data-editable="array" data-prop="cards">
    {cards.map((card) => (
      <div class="feature-image-card-grid__item" data-editable="array-item" data-id="page-sections/features/feature-image-card-grid/cardItem">
        <Card colorScheme="dark" backgroundColor="surface" rounded={false} border={false} paddingHorizontal="md" paddingVertical="md">
          <Image slot="before" source={card.image?.source} alt={card.image?.alt} aspectRatio="landscape" />
          <Heading level="h4" size="xs" text={card.cardEyebrow} data-prop="cardEyebrow" />
          <Heading level="h3" size="md" text={card.cardHeading} data-prop="cardHeading" />
          <Text text={card.cardDescription} data-prop="cardDescription" />
        </Card>
      </div>
    ))}
  </div>
</CustomSection>

<style is:global>
  @layer page-sections {
    .feature-image-card-grid__grid {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: var(--spacing-md);
      width: 100%;
      margin-top: var(--spacing-xl);
    }

    .feature-image-card-grid__item > .card {
      margin-top: 0;
    }

    .feature-image-card-grid__item .heading .heading-inner.size-xs {
      text-transform: uppercase;
      letter-spacing: 0.15em;
    }

    @media (max-width: 768px) {
      .feature-image-card-grid__grid {
        grid-template-columns: 1fr;
        max-width: 400px;
        margin-inline: auto;
      }
    }
  }
</style>
